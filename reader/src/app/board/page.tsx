/**
 * A2 行动看板（占位页）
 *
 * TODO(M2)：A2 便签聚合展示（贴墙），标注状态（待办/进行中/完成）
 * 详见 docs/详细设计.md 第 1.3.3 节
 */
export default function BoardPage() {
  return (
    <main className="flex-1 p-8">
      <h1 className="text-2xl font-bold">行动看板</h1>
      <p className="mt-2 text-sm opacity-70">
        TODO(M2)：A2 行动看板（待办/进行中/完成聚合）尚未实现。
      </p>
    </main>
  );
}
